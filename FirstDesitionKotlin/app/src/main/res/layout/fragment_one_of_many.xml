<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container_rel"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_above="@id/myFAB_right"
    android:clickable="false"
    tools:context="com.android.marco.firstdecision.fragments.OneOfManyFragment">

    <include layout="@layout/list_control_row" />

    <!--Spinner
        android:id="@+id/list_titles"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"/-->

    <ListView
        android:id="@+id/lv_oneofmany_things"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/rl_oneofmany_control"
        android:divider="@null"
        android:paddingTop="8dp" />

</RelativeLayout>
